[project]
name = "comment-bubbles-mvp"
version = "0.1.0"
description = "Dummy showcase for temporal comment bubble clustering"
requires-python = ">=3.11"
dependencies = [
  "fastapi==0.115.6",
  "uvicorn==0.30.6",
  "pydantic==2.10.4",
]

[tool.uvicorn]
